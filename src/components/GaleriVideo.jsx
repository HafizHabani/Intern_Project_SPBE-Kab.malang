import React from 'react';

const GaleriVideo = (props) => {
  // Logika komponen disini

  return (
    <div>
      <h1>Hello, {props.name}!</h1>
      <p>This is a functional component.</p>
    </div>
  );
};

export default GaleriVideo;
